<h1>All Superpowers</h1>

<ul>
  <% @powers.each do |power| %>
    <font size="5"><li><%= link_to power.name, power_path(power) %></li></font>
    <br>
  <% end %>
</ul>

<%= link_to 'Add new power', new_power_path %><br>
<%= link_to 'Home', home_path %>